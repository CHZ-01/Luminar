# Anonymous functions.
# Inline functions.
# Reduce lines of code.
# Syntax for lambda function.
# lambda arguments1, arguments2....:expression.

var = lambda a:a.capitalize()
print(var('asdf'))

var = lambda a,b:a+b
print(var(1,2))

cube = lambda x: x**3
print(cube(3))